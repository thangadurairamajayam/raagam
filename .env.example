# Optional: bake a YouTube Data API key into the build so everyone using your
# hosted copy can search without supplying their own.
# Restrict it by HTTP referrer in Google Cloud to your domain before deploying.
VITE_YOUTUBE_API_KEY=
